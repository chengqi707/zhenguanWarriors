// ============================================================
// 版本号（与 h5/package.json 保持一致，构建后自动跟随）
// ============================================================
import pkg from '../../package.json';

export const APP_VERSION = `v${pkg.version}`;
